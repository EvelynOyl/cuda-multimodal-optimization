# ═══════════════════════════════════════════════════════════════════════════
# requirements.txt — 公共基础依赖（Mac + Linux 通用）
# ═══════════════════════════════════════════════════════════════════════════
# 安装方式（选其一）：
#   Mac M5：        bash scripts/install_mac.sh
#   Linux CUDA 11.8： bash scripts/install_linux_cuda118.sh
#
# 下面只是"公共交集"——平台专属依赖写在对应脚本里

# ── 核心数值计算 ──────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0.0       # PyTorch / MLX 尚未全面适配 NumPy 2.x
torch>=2.1.0                # Mac 装 CPU/MPS 版; Linux 装 CUDA 版

# ── Transformers 生态 ──────────────────────────────────────────────────────
transformers>=4.40.0,<4.47.0   # LLaVA-1.5 官方推荐区间
tokenizers>=0.19.0
accelerate>=0.28.0
safetensors>=0.4.0

# ── 图像处理（LLaVA 视觉塔）────────────────────────────────────────────────
pillow>=10.0.0,<11.0.0
torchvision>=0.16.0         # CLIP 预处理依赖

# ── 推理服务（FastAPI）─────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
aiohttp>=3.9.0
httpx>=0.27.0               # 服务端健康检查 / 测试客户端

# ── 量化工具 ──────────────────────────────────────────────────────────────
datasets>=2.18.0
einops>=0.7.0
tqdm>=4.66.0

# ── 配置 / 日志 ───────────────────────────────────────────────────────────
pyyaml>=6.0

# ── 开发 / 测试（CI 需要）─────────────────────────────────────────────────
pytest>=7.4.0
pytest-benchmark>=4.0.0
pytest-asyncio>=0.23.0
