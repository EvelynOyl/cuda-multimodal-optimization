# ═══════════════════════════════════════════════════════════════════════════
# requirements.txt — Docker 镜像完整依赖（Linux + CUDA 11.8）
# ═══════════════════════════════════════════════════════════════════════════
#
# 版本锁定策略：
#   - PyTorch ≤ 2.1.2        （2.2+ 移除 CUDA 11.8 预编译包）
#   - vLLM   ≤ 0.3.3         （0.4+ 仅 CUDA 12.1 预编译）
#   - bitsandbytes ≤ 0.41.3   （0.42+ 仅 CUDA 12.x）
#   - xformers ≤ 0.0.23       （匹配 torch 2.1.x + CUDA 11.8）
#   - auto-gptq == 0.7.1      （CUDA 11.8 兼容，GPTQ 量化推理）
#
# 安装（在 Docker 内或服务器上）：
#   pip install torch==2.1.2 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════════════

# ── PyTorch（CUDA 11.8 预编译）─────────────────────────────────────────────
# 注意：torch/torchvision/torchaudio 需指定 --index-url，此处仅声明版本
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# ── vLLM 推理引擎 ─────────────────────────────────────────────────────────
vllm==0.3.3
ray>=2.9.0
nvidia-ml-py>=12.535

# ── CUDA 11.8 专用量化 ────────────────────────────────────────────────────
bitsandbytes==0.41.3
xformers==0.0.23.post1
auto-gptq==0.7.1

# ── Transformers 生态 ──────────────────────────────────────────────────────
transformers==4.46.3
tokenizers>=0.19.0
accelerate==0.28.0
safetensors>=0.4.0

# ── 图像处理（LLaVA 视觉塔）────────────────────────────────────────────────
pillow>=10.0.0,<11.0.0

# ── 推理服务（FastAPI）─────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
aiohttp>=3.9.0
httpx>=0.27.0

# ── 量化 / 数据处理工具 ────────────────────────────────────────────────────
datasets>=2.18.0
einops>=0.7.0
tqdm>=4.66.0

# ── 基础数值 ──────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0.0

# ── 配置 / 日志 / 监控 ────────────────────────────────────────────────────
pyyaml>=6.0
psutil>=5.9.0
prometheus-client>=0.19.0

# ── 测试（打包在镜像中便于 CI）─────────────────────────────────────────────
pytest>=7.4.0
pytest-benchmark>=4.0.0
pytest-asyncio>=0.23.0

# ── Flash Attention 2（可选，需源码编译，10-15 分钟）───────────────────────
# CUDA 11.8 无预编译包。如需启用，构建前取消下行注释：
# flash-attn==2.3.6
